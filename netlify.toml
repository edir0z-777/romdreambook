[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  path = "/api/webhook"
  function = "webhook"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  
[headers]
  for = "/thank-you"
    [headers.values]
    Cache-Control = "no-store, must-revalidate"
    X-Robots-Tag = "noindex"